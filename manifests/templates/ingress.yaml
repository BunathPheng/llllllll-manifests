---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: llllllll                  # Ingress name
  labels:
    app: llllllll
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "llllllll.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: llllllll               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
